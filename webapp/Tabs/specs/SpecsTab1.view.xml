<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    core:require="{formatter: 'ui5basicspractice/project1/model/formatter'}"
>
    <Panel>
        <FlexBox
            height="100px"
            alignItems="Center"
            justifyContent="Center"
        >
            <Table
                id="carSpecsTable1"
                inset="false"
                items="{
                                path: 'manifestCarModel>/Cars',
                                sorter: {
                                    path: 'DOM'
                                }
                            }"
            >
                <columns>
                    <Column width="12em">
                        <Text text="Car" />
                    </Column>

                    <Column>
                        <Text text="Cylinder" />
                    </Column>
                    <Column>
                        <Text text="Power" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem vAlign="Middle">
                        <cells>
                            <Text text="{manifestCarModel>car}" />
                            <Text text="{manifestCarModel>Engine/0/cylinder}" />
                            <Text text="{manifestCarModel>Engine/0/power}" />
                            <!-- <Text
                                                text="{
                                                    path: 'manifestCarModel>ownership',
                                                    formatter: 'formatter.carOwnershipText'
                                                }"
                                            /> -->
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </FlexBox>
    </Panel>
</mvc:View>
